s, terminal transitions, and reopen behavior. [`lifetxt/ticket_activity.py`](lifetxt/ticket_activity.py) defines append-only `record:ticket_event` and `record:time_entry` Notes with stable IDs, ticket parents, UTC timestamps, per-ticket sequences, transaction/source-revision context, changed-field summaries, provider/reference fields, configured activities, immutable correction relationships, and TK020-TK039 integrity diagnostics. [`lifetxt/ticket_activity_mutation.py`](lifetxt/ticket_activity_mutation.py) allocates IDs under the shared sidecar lock and commits the current ticket plus required event and optional time entry in one exact-revision atomic replacement; dry runs calculate the full post-write revision and duplicate transactions are rejected. The CLI adds `ticket workflow|transition|comment|reassign|change|watch|unwatch|activity|log-time|time|validate-history`; legacy mutation commands remain compatibility paths and are not claimed as event-producing. [`lifetxt/ticket_planning.py`](lifetxt/ticket_planning.py) and [`lifetxt/ticket_planning_mutation.py`](lifetxt/ticket_planning_mutation.py) add `record:version`, `record:sprint`, version/sprint state graphs, same-project membership, unresolved-member release/close guards, backlog/roadmap views, optional story-point capacity warnings, and history-preserving `ticket plan`; `version` and `sprint` CLI families expose the operations. Ticket views, read-only MCP, and capability discovery consume these contracts through [`lifetxt/ticket_workflow_surfaces.py`](lifetxt/ticket_workflow_surfaces.py). `schema_extensions_v18` publishes seven schemas and raises the generated bundle to 64 documents. The atomic claim is deliberately limited to records in one authoritative life.txt file; split sources and remote writes remain disabled.
 
 - [ ] Enforce workflow roles, transition-specific editable/visible fields, ticket/custom-field privacy, project membership, and administrative overrides from authenticated Web/remote identities rather than caller-supplied local role strings. Add workflow/config schema migration, unsupported-version refusal, alias resolution, permission-change races, and complete allowed/forbidden transition matrices for each tracker/project profile.
-- [ ] Add revision-set and journal-backed recovery for split ticket/event/time/planning sources, notification stores, timer/work-session state, all-or-none bulk transitions, relation events, attachments, and project archive. Publish stable transaction IDs, exact touched revisions, generated records, inspect/resume/compensate/abandon actions, crash-boundary evidence, and one-winner/one-conflict retry behavior before enabling any remote workflow write.
+- [ ] Extend the implemented exact ticket revision-set and journal-backed recovery contract to notification/delivery stores, timer/work-session state, reciprocal relation events, attachment references/packages, version/sprint lifecycle side records, and project archive. Preserve the implemented stable transaction IDs, exact touched revisions, generated-record previews, inspect/resume/compensate/abandon actions, and one-winner/one-conflict behavior; add crash-boundary and adverse-filesystem evidence for each newly admitted side store before enabling the corresponding remote workflow write.
 - [ ] Add explicit append-only administrative history operations for comment/body redaction, supersede, provider-history import, provenance verification, retention, compaction, and export. Never silently rewrite old events or correction entries; preserve hashes/IDs and record the authorized reason, actor, policy, and replacement relationship.
 - [ ] Implement watcher expansion and notification policy for transition, assignment, comment, due-date, relation/blocker, pull-request, build, version, close, and reopen events. Resolve users/groups deterministically, preserve original watcher references and resolved recipients, redact private fields, isolate delivery state from ticket completion, and recover partial delivery independently from the ticket/event commit.
 - [ ] Integrate the existing timer and compound work-session contracts through explicit proposals or revision-set transactions that can create time entries without double-counting legacy `elapsed:`. Add start/stop/resume/cancel/recovery mapping, timer/work-session references, correction approval, project/user/activity timesheets, estimate/remaining/variance reports, billing/export policy, and migration guidance.
@@ -170,22 +170,22 @@ The local same-file foundation is implemented. [`lifetxt/ticket_workflow.py`](li
 
 ## P1: Remote Safe Mode and Remote Workspace Access
 
-This track starts only after revision migration, authoritative write routing, stable schemas, versioned configuration/workspaces, and the public-deployment security review are complete. The first release is single-user and does not attempt offline synchronization or automatic merging.
-
-- [ ] Add single-user Remote Safe Mode with password login and/or trusted reverse-proxy authentication while retaining scoped token authentication for API clients.
-- [ ] Use secure server-side sessions, protected cookies, CSRF protection for browser writes, login throttling, session expiration/revocation, security headers, environment-backed secrets, and auditable authentication events.
-- [ ] Derive capability optional-feature availability from installed dependencies and configuration. Include package/server/policy/schema/transaction/configuration/ticket/workflow/event versions and publish older-client compatibility rules.
-- [ ] Define a shared `WorkspaceBackend` interface with `LocalFileBackend` and `RemoteApiBackend` so CLI/TUI commands do not reimplement local-versus-remote behavior.
-- [ ] Add `lifetxt remote add|list|show|test|remove NAME`. Store non-secret profile values under the published remote-profile schema and reference credentials through environment variables or OS credential facilities.
-- [ ] Add read-only remote operations for list, show, filter, agenda, next, review, messages, status, links, graph, diagnostics, completion, projects, portfolios, tickets, ticket activity, versions, sprints, backlog, workspace source inspection, and doctor compatibility inspection.
-- [ ] Add `lifetxt tui --remote NAME` with read-only browsing first. Reuse normal TUI rendering, commands, timezone context, diagnostics, ticket queries, and capability negotiation.
-- [ ] Add conflict-aware remote create/update/delete/done/message/status/acknowledgement only after observe mode is retired. Defer timer, attachment, archive, project creation/archive, workspace configuration, ticket create/edit/transition/comment/time/bulk, and undo remote writes until capability entries report complete multi-target, permission, event-generation, and recovery enforcement.
-- [ ] Validate remote conflicts against `conflict-v1.schema.json` and show expected revision, current revision, current item, attempted change, generated ticket event, and affected side records. Never overwrite automatically or call a comparison a three-way merge without a retained base.
-- [ ] Start refresh with explicit reload and bounded polling. Add SSE/WebSocket only after reconnect, ordering, backpressure, missed-event behavior, ticket event ordering, and notification delivery are specified and polling is demonstrably inadequate.
-- [ ] Keep remote/local transfer explicit through export, copy, or proposal import. Do not add background bidirectional synchronization in this track.
-- [ ] Add diagnostics for TLS, authentication, authorization, clock skew, schema/capability/configuration/ticket-workflow mismatch, read-only mode, proxy configuration, ETag removal/rewrite, transaction-version mismatch, and server recovery state.
-- [ ] Test remote CLI/TUI against stale revisions, expired sessions, permission changes, interruption, retries, multi-file workspaces, older servers, required mode, proxies that alter ETags, servers recovering an interrupted transaction,