# Python Profile

Use this profile for Python projects.

- Prefer the version declared by the downstream project.
- Prefer `pyproject.toml` for tool configuration.
- Use commands from `.ai/project/COMMANDS.yml`.
- Keep virtual environments out of Git.

Downstream projects should define formatting, linting, type checking where practical, unit tests, and integration tests when external boundaries exist.
