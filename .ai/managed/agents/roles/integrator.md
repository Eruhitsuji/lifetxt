# Integrator Role

The Integrator owns shared files such as lockfiles and registries, resolves conflicts with semantic understanding, validates integration branches, ensures CI passes, and retires worktrees and branches after merge.

The Integrator must not hide behavioral changes inside conflict resolution.
