# Decisions

| Date | Decision | Owner | Alternatives | Reason | Follow-up |
| --- | --- | --- | --- | --- | --- |
| 2026-08-06 | Protect all three remaining unprotected `os.replace` call sites (atomic.py:73, config_writer.py:96, config_writer.py:125), not just the shared commit primitive alone | Eruhitsuji | Protect only atomic.py:73 and leave config_writer.py's rotation as-is | These are the only unprotected `os.replace` sites left in `lifetxt/` (grep-confirmed against the whole tree); leaving two of three unprotected would repeat the #86 mistake of fixing only the site that already failed | None |
| 2026-08-06 | Reuse `transaction_journal.py`'s exact retry policy values (Windows-only, initial + 4 retries at 0.01/0.05/0.1/0.25s) for the new call sites | Eruhitsuji | Design an independent retry budget per call site | Requirement 3 demands identical, predictable behavior across all covered paths; the existing values are already accepted precedent (#86 / #94) | None |
| 2026-08-06 | On retry-budget exhaustion, configuration backup/rejected-candidate rotation stays fully silent (no new log, diagnostic, or return-value change) | Eruhitsuji | Surface exhaustion via a log line, `lifetxt doctor` diagnostic, or return value | Rotation is documented best-effort recovery, not correctness-critical; it already swallows any `OSError` including today's un-retried `PermissionError`, so this preserves existing behavior rather than introducing new observability scope | Would need a new requirement if revisited |
| 2026-08-06 | Shared retry helper (`replace_with_retry`) lives in `lifetxt/atomic.py`; `transaction_journal.py` is not refactored to share it | Claude Code (design phase) | (1) duplicate the retry loop at each new call site, (2) new dedicated module, (3) refactor `transaction_journal.py` to delegate to the same helper | `atomic.py` has zero internal dependencies and `config_writer.py` already depends on it, so it is the only module both new call sites can use without a cycle; refactoring `transaction_journal.py` would touch incident-hardened, fault-injection-tested code that `change.yml` places in forbidden scope | A future spec could migrate `transaction_journal.py` onto the shared helper; not this change |
| 2026-08-07 | Decompose the fix into two GitHub Issues — (1) add the retry helper, (2) wire it into both call sites plus regression validation — rather than one combined issue or three per-site issues | Claude Code (task decomposition, per #108) | Single M-sized issue covering helper + both wiring sites; three issues splitting each wiring site separately | Complexity score for the combined work is 5 (S/M band, "split if possible"); the helper is an independently reviewable, inert addition that can land first, while the two wiring sites are one-line changes each whose *combined* regression run is what proves the Requirement 3 consistency guarantee — splitting them further would fragment that proof without reducing review burden | None |
