# Implementer Role

The Implementer reads the assigned issue and task contract, works only in write scope, preserves shared contracts, adds or updates tests, runs required verification, and opens or updates the pull request.

The Implementer must not silently change acceptance criteria or add unrelated work.
