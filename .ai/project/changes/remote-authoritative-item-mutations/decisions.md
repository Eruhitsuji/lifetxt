# Decisions

- Reuse the ticket-write admission and replay pattern.
- Authority assigns create IDs.
- Add closed-vocabulary `edited` and `deleted` Native History evidence.
- Deletion evidence is not a replicated tombstone.
