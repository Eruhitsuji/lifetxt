schema_version: 1

links:
  - requirement_id: req-lifetxt-self-update
    capability_id: cap-lifetxt-self-update
    epic: null
    task_issue: https://github.com/Eruhitsuji/lifetxt/issues/206
    pull_request: https://github.com/Eruhitsuji/lifetxt/pull/207
    tests_or_evidence:
      - "tests/test_lifetxt.py LifeTxtUpdateCommandCliTests (15 tests, subprocess.run mocked): dry run reports without merging, --yes merges, already-up-to-date short-circuits, dirty tree refused, detached HEAD refused, non-git install refused, fetch failure fails loudly, merge failure fails loudly, missing git executable fails loudly, git timeout fails loudly, option-like ref/remote rejected, no-ref-and-no-release does nothing, explicit --ref skips the GitHub API entirely, argparse wiring"
      - python -m unittest tests.test_lifetxt.LifeTxtUpdateCommandCliTests tests.test_lifetxt.LifeTxtUpdateCheckCliTests -> Ran 29 tests, OK
      - "live: real disposable clone of this repository, checked out at an old commit on a real branch -- dry run correctly reported the pending fast-forward; --yes correctly fast-forwarded (confirmed via git log); a second run correctly reported already up to date; dirty-working-tree refusal and detached-HEAD refusal each confirmed against the same real clone"
      - "live: the original implementation crashed with UnicodeDecodeError decoding git output against a real Windows ja-JP-locale directory tree; fixed by decoding explicitly as UTF-8 with errors=\"replace\"; re-verified clean after the fix"
      - ruff format --check lifetxt tests scripts
      - ruff check lifetxt tests scripts -> All checks passed
      - python -m lifetxt check examples/minimal_life.txt -> OK: 10 item(s)
    release_or_deployment: null
    status: implemented

gaps: []
