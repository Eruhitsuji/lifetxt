{
  "baseline_version": 1,
  "description": "Reviewed pre-existing direct write call shapes. Line numbers are intentionally excluded so harmless edits do not churn the baseline; new path/call pairs fail the release gate.",
  "allowed": [
    {
      "path": "lifetxt/cli.py",
      "call": "open(w)",
      "reason": "Existing undo/backup cache snapshots and JSON configuration output. Authoritative life.txt writes remain routed through mutation; config output should move to a dedicated atomic config writer in a future cleanup."
    },
    {
      "path": "lifetxt/safety_foundation.py",
      "call": "open(w)",
      "reason": "Generated JSON Schema publication output, not an authoritative life.txt/state mutation."
    },
    {
      "path": "lifetxt/transaction_journal.py",
      "call": "os.replace",
      "reason": "Durable transaction-journal primitive. The implementation fsyncs the temporary file, atomically replaces the journal/artifact path, and fsyncs the parent directory so interrupted multi-target operations remain recoverable. This reviewed low-level operational-state writer is intentionally separate from authoritative life.txt mutation routing."
    },
    {
      "path": "lifetxt/config_writer.py",
      "call": "os.replace",
      "reason": "Bounded backup rotation in the dedicated atomic configuration writer. This is the reviewed home the write-route baseline previously called for: config writes validate against config-v1.schema.json, keep a bounded .bak rotation via os.replace, and commit atomically through atomic_write_text. It writes JSON configuration, not authoritative life.txt/state, so it is intentionally separate from mutation routing."
    },
    {
      "path": "lifetxt/sqlite_codec.py",
      "call": "atomic_write_bytes",
      "reason": "SQLite interchange export (#691) builds the complete database at a private temporary path first and only commits the finished bytes via the shared atomic-replace primitive. The destination is a new binary export file the caller names with -o, never authoritative life.txt/state, so the higher-level conflict-aware mutation.py wrapper (built for concurrently-editable text records) does not apply; atomic_write_bytes alone already guarantees no partial destination on failure."
    },
    {
      "path": "lifetxt/lifetxtz_codec.py",
      "call": "atomic_write_bytes",
      "reason": "The .lifetxtz compressed archive export (#693) is built entirely in memory and committed via the shared atomic-replace primitive, matching sqlite_codec.py's own reviewed rationale above: the destination is a new binary export file, not authoritative life.txt/state, and atomic_write_bytes alone already guarantees no partial destination on failure."
    }
  ]
}
