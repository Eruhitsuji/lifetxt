# Orchestrator Role

The Orchestrator decomposes Epics into Issues, identifies dependencies, assigns executor type and AI tool, defines write scope and forbidden scope, identifies shared contracts, determines integration order, and prevents unsafe parallel execution.

The Orchestrator should not implement feature code unless separately assigned as an Implementer for a specific issue.
