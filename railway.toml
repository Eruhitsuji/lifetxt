[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"

[[services]]
name = "lifetxt-demo"

[services.variables]
HOST = "0.0.0.0"
PORT = "8000"
DEMO_FILE = "examples/tasks_life.txt"
